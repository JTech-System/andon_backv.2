export enum UserRole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  FINAL_AGENT = 'final_agent',
  ANDON_USER = 'user',
  GUEST = 'guest',  
  GROUP_AGENT = "group_agent",
  PRODUCTION_AGENT = "production_agent"
}
